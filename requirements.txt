# Ядро
anthropic>=0.40.0        # клиент Claude API (агент-цикл, tool use) — понадобится с Фазы 1

# Системная работа под Windows
send2trash>=1.8.3        # безопасное удаление в корзину (не прямое!)
psutil>=6.0.0            # инфо о системе/дисках

# Вывод в консоли
rich>=13.7.0             # красивый CLI-вывод (таблицы, дерево, подтверждения)

# Конфиг
python-dotenv>=1.0.0     # чтение .env с API-ключом

# Сервер для Electron-версии (Фаза 5)
fastapi>=0.110.0
uvicorn>=0.27.0
websockets>=12.0
